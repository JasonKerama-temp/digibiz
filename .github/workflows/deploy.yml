name: 🚀 Cordova iOS Production Build (FINAL FIX)

on:
  push:
    branches:
      - main

env:
  APP_NAME: "Digibiz"
  PROJECT_VERSION: 3080 
  TEAM_ID: Z6XW557ZX2 
  PROFILE_NAME: "Digibiz iOS"
  NODE_VERSION: 20
  IOS_PLATFORM_VERSION: 7.0.0 

jobs:
  build_ios:
    runs-on: macos-latest

    steps:
      - name: 🔍 Checkout repository
        uses: actions/checkout@v4

      # --- 1. SETUP ---
      - name: 🛠️ Setup Node.js (v${{ env.NODE_VERSION }})
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          # Removed 'cache: npm' to prevent lock file dependency issues

      - name: 📦 Install npm dependencies (Force fresh resolution)
        run: |
          # 🛑 CRITICAL FIX: Ensures a clean install without relying on npm ci or external cache
          npm cache clean --force
          rm -rf node_modules
          npm install

      # Force Install Cordova CLI 12 and verify
      - name: 🔑 Install Cordova CLI 12
        run: |
          echo "Installing Cordova 12 globally..."
          npm install -g cordova@12 --force
          cordova -v
          if ! cordova -v | grep -q "12"; then 
            echo "❌ Cordova 12 failed to install. Exiting."
            exit 1 
          fi

      # --- 2. PLATFORM & PLUGIN PREP (Includes Aggressive Cleanup) ---
      - name: 🧹 Clean CocoaPods Cache and Project Directories
        run: |
          # Aggressively delete the project-specific iOS folders
          rm -rf platforms/ios || true
          rm -rf plugins || true
          # Delete the global CocoaPods cache to prevent stale dependency conflicts
          rm -rf $HOME/Library/Caches/CocoaPods || true
          
      - name: 📱 Add iOS platform v${{ env.IOS_PLATFORM_VERSION }} & Prepare
        run: |
          echo "🔄 Adding platform..."
          # The config.xml fixes (iOS 13.0 and Firebase versions) are now critical here
          cordova platform add ios@${{ env.IOS_PLATFORM_VERSION }}
          
          # This runs 'pod install' and other plugin hooks
          echo "🔄 Running Cordova prepare (and pod install)..."
          cordova prepare ios
          
          echo "✅ Setting permissions..."
          chmod -R +x platforms/ios/cordova

      # --- 3. SIGNING ASSET INSTALLATION ---
      # (All subsequent steps are unchanged)
      # 🔑 Import Certificate (P12)
      - name: 🔑 Import iOS Certificate
        run: |
          echo "📦 Importing certificate..."
          if [ -z "${{ secrets.IOS_CERTIFICATE_P12 }}" ]; then exit 1; fi
          if ! echo "${{ secrets.IOS_CERTIFICATE_P12 }}" | base64 -d > ios_cert.p12 2>/dev/null; then exit 1; fi
          security create-keychain -p "" build.keychain || true
          security list-keychains -s ~/Library/Keychains/build.keychain
          security unlock-keychain -p "" ~/Library/Keychains/build.keychain
          security import ios_cert.p12 -k ~/Library/Keychains/build.keychain -P "${{ secrets.IOS_CERTIFICATE_PASSWORD }}" -T /usr/bin/codesign
          security default-keychain -s ~/Library/Keychains/build.keychain
          echo "✅ Certificate imported."

      # 📄 Install Provisioning Profile
      - name: 📄 Install Provisioning Profile
        run: |
          echo "📝 Installing provisioning profile..."
          mkdir -p "$HOME/Library/MobileDevice/Provisioning Profiles"
          PROFILE_DEST="$HOME/Library/MobileDevice/Provisioning Profiles/${{ env.PROFILE_NAME }}.mobileprovision"
          if ! echo "${{ secrets.IOS_PROVISIONING_PROFILE }}" | base64 --decode > "$PROFILE_DEST" 2>/dev/null; then 
            echo "❌ Failed to decode profile."
            exit 1
          fi
          echo "✅ Profile installed."

      # --- 4. BUILD & EXPORT ---
      # 🏗️ Build Archive with Manual Signing
      - name: 🏗️  Build Xcode Archive
        run: |
          echo "🔨 Building archive with manual signing..."
          mkdir -p output
          export KEYCHAIN_PATH="$HOME/Library/Keychains/build.keychain"
          
          # CRITICAL: Target 'App.xcworkspace'
          xcodebuild archive \
            -workspace "platforms/ios/App.xcworkspace" \
            -scheme "${{ env.APP_NAME }}" \
            -configuration Release \
            -destination "generic/platform=iOS" \
            -archivePath "output/${{ env.APP_NAME }}.xcarchive" \
            -derivedDataPath "output/derived" \
            CODE_SIGN_STYLE="Manual" \
            CODE_SIGNING_REQUIRED="YES" \
            CODE_SIGN_IDENTITY="iPhone Developer" \
            PROVISIONING_PROFILE_SPECIFIER="${{ env.PROFILE_NAME }}" \
            DEVELOPMENT_TEAM="${{ env.TEAM_ID }}" \
            CURRENT_PROJECT_VERSION="${{ env.PROJECT_VERSION }}"
          
          echo "✅ Archive created."

      # 📤 Export IPA
      - name: 📤 Export IPA
        run: |
          echo "📦 Exporting IPA..."
          mkdir -p output/ipa
          xcodebuild -exportArchive \
            -archivePath "output/${{ env.APP_NAME }}.xcarchive" \
            -exportOptionsPlist exportOptions.plist \
            -exportPath "output/ipa/" \
            -allowProvisioningUpdates
          echo "✅ IPA exported."

      # --- 5. CLEANUP & ARTIFACTS ---
      # 🧹 Cleanup Sensitive Files (Always run)
      - name: 🧹 Cleanup Sensitive Files
        if: always()
        run: |
          echo "🗑️ Cleaning up sensitive files..."
          security delete-keychain build.keychain || true
          rm -f ios_cert.p12 || true
          rm -rf "$HOME/Library/MobileDevice/Provisioning Profiles/${{ env.PROFILE_NAME }}.mobileprovision" || true
      
      # 📤 Upload IPA Artifact
      - name: 📤 Upload IPA Artifact
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: ios-ipa-final-${{ github.run_number }}
          path: output/ipa/*.ipa
          retention-days: 30